"""
This module provides functions to perform user recognition for the chatbot.

EVERYTHING BELOW IS TEMPORARY for now

Functions in this module include:
- recognize_user(image): takes an image and returns the recognized user's name or 'unknown' if not recognized
"""

def recognize_user(self, image):
    """
    Given an image, recognizes the user in the image.
    
    Args:
        image (bytes): The input image as bytes.
    
    Returns:
        user (str): The recognized user.
    """
    pass
