def predict_user(self, message):
    """
    Given a message, predicts the most likely user.
    
    Args:
        message (str): The content of the message.
    
    Returns:
        user (str): The predicted user.
    """
    pass